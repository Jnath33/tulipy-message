<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fr.jnath.android.tulipy.messager">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TulipYMessage"
        tools:targetApi="31">
        <activity
            android:name=".MainMenu"
            android:exported="false" />
        <activity
            android:name=".SeeMessage"
            android:exported="false"
            android:label="@string/title_activity_see_message"
            android:theme="@style/Theme.TulipYMessage.NoActionBar" />
    </application>

</manifest>